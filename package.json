{
  "name": "tomato",
  "title": "pomodoro",
  "version": "1.0.0",
  "description": "番茄钟",
  "icon": "src/assets/images/icon.svg",
  "main": "src/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build:mac": "electron-packager ./ pomodoro --platform=darwin --arch=x64 --icon=./src/assets/images/icon.svg --out=./dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^7.1.7",
    "electron-reloader": "^1.2.1"
  },
  "dependencies": {
    "timer.js": "^1.0.4"
  }
}
